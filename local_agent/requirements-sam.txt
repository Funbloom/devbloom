# Optional: UI Breakdown SAM (install into the shared root .venv — same as Mesh Gen).
# From the repo root:
#   pip install -r requirements.txt
#   pip install -r local_agent/requirements-sam.txt
#
# See README-SAM.md for checkpoints and SAM_CHECKPOINT_PATH.

torch>=2.1.0
torchvision>=0.16.0
opencv-python-headless>=4.8.0
numpy>=1.24.0
pillow>=10.0.0
pycocotools>=2.0.6
git+https://github.com/facebookresearch/segment-anything.git
