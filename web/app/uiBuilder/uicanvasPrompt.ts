/** Max style-only reference images (upload / gallery); server uses the same cap. */
const MAX_UI_STYLE_REFERENCE_IMAGES = 3;

export function maxUiStyleReferenceImages(): number {
  return MAX_UI_STYLE_REFERENCE_IMAGES;
}
