"use client";

import { PipelinePageContent } from "../../[gameKey]/pipelines/[pipelineKey]/page";

export default function PocketVoyagerCitiesPage() {
  return <PipelinePageContent gameKey="pocket_voyager" pipelineKey="cities" />;
}
